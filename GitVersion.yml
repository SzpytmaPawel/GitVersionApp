mode: ContinuousDeployment
branches:
  master:
    regex: master
    increment: Minor
  develop:
    regex: develop
    increment: Patch
  feature:
    regex: feature[/-]
    increment: Patch
  hotfix:
    regex: hotfix[/-]
    increment: Patch
ignore:
  sha: []
commit-message-incrementing: Enabled